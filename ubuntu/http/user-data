#cloud-config
autoinstall:
  version: 1

  #early-commands:
  #  - snap set system proxy.http="http://proxy.example.com:8080"
  #  - snap set system proxy.https="http://proxy.example.com:8080"

  locale: en_US
  keyboard:
    layout: jp

  identity:
    hostname: ubuntu-server
    username: vsphere
    # password can be generated by 'openssl passwd -6 xxx'
    password: "$6$9w.Mv.rtLPkk/Szv$rDknDsx4ic.0IXxz8WrwYy4LNSpR6cOpYlcRZve.KuRvHACOyCPY.sHLVTI957sJR.oqq1uJxhU8OtHnrjO4p/"

  ssh:
    install-server: true
    allow-pw: true
    authorized-keys: []

  late-commands:
    - |
      echo 'vsphere        ALL=(ALL)       NOPASSWD: ALL' > /target/etc/sudoers.d/vsphere
